#!/usr/bin/python3
# add two tuples


def add_tuple(tuple_a=(), tuple_b=()):
    for i in range(0, 2):
        if ( not (a[i])):
            a[i] = 0
        if ( not (b[i])):
            b[i] = 0
        sum_tuple[i] = a[i] + b[i]
    ret_tuple() = tuple(sum_tuple)
