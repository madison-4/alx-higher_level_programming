#!/usr/bin/python3
"""A module to get the isinancefunction
"""


def is_same_class(obj, a_class):
    """clones is instance
    """

    if (type(obj) is a_class):
        return (True)
    else:
        return (False)
